import React from "react";

const AssumptionsForm = (props) => {
  // current age
  // retirement age
  // monthly expenses
  // current savings
  // investment return
  // inflation rate
  // tax rate
  return <span>{JSON.stringify(props.assumptions)}</span>;
};

export default AssumptionsForm;
